# 🔑 APIKEY-king OpenRouter支持测试配置
# 复制此文件为 .env 并填入真实的配置值

# ==== 必填配置 ====
# GitHub访问令牌（必需）
# 获取方式：https://github.com/settings/tokens
# 权限要求：public_repo
GITHUB_TOKENS=your_github_token_here

# ==== 基础配置 ====
# 数据存储目录
DATA_PATH=./data

# 代理设置（可选，建议使用）
# 格式：http://user:pass@host:port 或 http://host:port
# 多个代理用逗号分隔
# PROXY=http://localhost:1080

# ==== ModelScope 配置 ====
# 目标API地址（检测这些地址的密钥）
TARGET_BASE_URLS=https://api-inference.modelscope.cn/v1/,api-inference.modelscope.cn

# 仅提取ModelScope密钥，不验证（推荐）
MODELSCOPE_EXTRACT_ONLY=true

# ==== OpenRouter 配置 ====
# OpenRouter API地址（检测这些地址的密钥）
OPENROUTER_BASE_URLS=https://openrouter.ai/api/v1,openrouter.ai

# 仅提取OpenRouter密钥，不验证（推荐）
OPENROUTER_EXTRACT_ONLY=true

# 可选：OpenRouter宽松匹配配置（召回不足时再开启）
# OPENROUTER_USE_LOOSE_PATTERN=true
# OPENROUTER_PROXIMITY_CHARS=800
# OPENROUTER_REQUIRE_KEY_CONTEXT=false

# ==== 扫描配置 ====
# 仓库年龄过滤（天数）
DATE_RANGE_DAYS=730

# 查询配置文件路径
QUERIES_FILE=queries.txt

# Gemini验证模型
HAJIMI_CHECK_MODEL=gemini-2.5-flash

# 文件路径黑名单（跳过这些类型的文件）
FILE_PATH_BLACKLIST=readme,docs,doc/,.md,example,sample,tutorial,test,spec,demo,mock