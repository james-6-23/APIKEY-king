# APIKEY-king 配置文件模板
# 复制此文件为 .env 并填入真实的配置值
# 推荐使用 .env.simple 作为更简化的配置模板

# ==== 必填配置 ====
# GitHub访问令牌（必需）
# 获取方式：https://github.com/settings/tokens
# 权限要求：public_repo
# 支持多个token，用逗号分隔
GITHUB_TOKENS=your_github_token_here

# ==== 推荐配置 ====
# 代理设置（强烈推荐，避免IP被封）
# 格式：http://user:pass@host:port 或 http://host:port
# 多个代理用逗号分隔
PROXY=http://localhost:1080

# 数据存储目录
DATA_PATH=./data

# 扫描时间范围（天数，默认730天=2年）
DATE_RANGE_DAYS=730

# ==== 验证器配置（默认全部启用）====
# 如需节省成本，可以关闭某些验证器
GEMINI_VALIDATION_ENABLED=true
OPENROUTER_VALIDATION_ENABLED=true
MODELSCOPE_VALIDATION_ENABLED=true
SILICONFLOW_VALIDATION_ENABLED=true

# ==== 验证器模型配置 ====
# Gemini 验证模型（推荐使用快速模型）
HAJIMI_CHECK_MODEL=gemini-2.5-flash
GEMINI_TIMEOUT=30.0

# OpenRouter 验证模型（推荐使用免费模型）
OPENROUTER_TEST_MODEL=deepseek/deepseek-chat-v3.1:free
OPENROUTER_TIMEOUT=30.0

# ModelScope 验证模型（推荐使用轻量模型）
MODELSCOPE_TEST_MODEL=Qwen/Qwen2-1.5B-Instruct
MODELSCOPE_TIMEOUT=30.0

# SiliconFlow 验证模型（推荐使用高效模型）
SILICONFLOW_TEST_MODEL=Qwen/Qwen2.5-72B-Instruct
SILICONFLOW_TIMEOUT=30.0

# ==== 高级配置（通常不需要修改）====
# 文件路径黑名单（跳过这些类型的文件）
FILE_PATH_BLACKLIST=readme,docs,doc/,.md,example,sample,tutorial,test,spec,demo,mock

# 查询配置文件路径（系统会自动选择）
# QUERIES_FILE=config/queries/default.txt